import { SearchOrdersPipe } from './search-orders.pipe';

describe('SearchOrdersPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchOrdersPipe();
    expect(pipe).toBeTruthy();
  });
});
